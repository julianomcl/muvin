<nav class="navbar navbar-fixed-top navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <%= link_to 'Muvin', root_path, class: "navbar-brand" %>
        </div>
        <ul class="nav navbar-nav">
            <li><%= unless is_user_logged_in?
                    link_to 'Cadastre-se', sign_up_path end%>
            </li>
            <li><%= unless is_user_logged_in?
                    link_to 'Login', login_path end%>
            </li>
            
            <li class="dropdown">
            <%= if is_user_logged_in? and session[:hash].nil?
                    link_to 'Sincronizar com Spotify', '/auth/spotify', class: "dropdown-toggle" end%>
            </li>
            
            <li><%= if is_user_logged_in?
                    link_to 'Logout', logout_path end%>
            </li>
        </ul>
    </div>
</nav>
