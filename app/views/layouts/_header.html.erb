<%= link_to 'Home', root_path %>
<%= if !is_user_logged_in? then
    link_to 'Cadastre-se', sign_up_path
    end%>
<%= if !is_user_logged_in? then
    link_to 'Login', login_path 
    end%>
<%= if is_user_logged_in? then
    link_to 'Minhas Configurações', mysettings_path
    end%>
<%= if is_user_logged_in? then
    link_to 'Logout', logout_path 
    end%>